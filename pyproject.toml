[project]
name = "moesekai"
version = "0.0.1"
description = "访问moesekai截图的插件"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "astrbot>=4.0.0",
    "asyncio",
    "orjson",
    "playwright",
    "Pillow",
]
